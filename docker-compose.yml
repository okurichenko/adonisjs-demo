version: '2'
services:
  web:
    build: .
    ports:
     - "3333:3333"
    volumes:
     - ./app:/usr/src/app
    depends_on:
     - mongo
  mongo:
    image: mongo
    volumes:
     - ./mongo-data:/data/db
